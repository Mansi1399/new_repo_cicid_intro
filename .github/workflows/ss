name: ss
on:
 push:
  branches:
   - main
 workflow_dispatch:


jobs:
  substring:
    runs-on: ubuntu-latest
    steps:
    
          
      - name: Print Substring
        run: echo "Extracted Substring: h"
