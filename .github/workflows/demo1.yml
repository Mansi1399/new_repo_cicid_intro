name: demo1

on:
 push:
  barnches:
    - master
 workflow_dispatch:

jobs:
 build:
  name: build and test
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v2
      name: 'Checkout repository'
      
    - name: hello
      run:  echo $GITHUB_WORKSPACE

